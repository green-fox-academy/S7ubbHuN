#include <stdio.h>
#include <stdlib.h>

int main()
{
    int soldiers_number, pos;
    printf("Enter the number of soldiers: ");
    scanf("%d", &soldiers_number);
    if (soldiers_soldiers_number < 1) {
            printf("This is not a positive number");
            exit(0);
    }
    pos = 2;

    printf("The chosen place is %d to survive", circle(soldiers_number, pos));
    return 0;
}

int circle(soldiers_number, pos)
{


}


